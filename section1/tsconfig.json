{
  "compilerOptions": {
    "target": "ESNext", //컴파일 되는 JS의 파일 버전
    "module": "ESNext", //자바스크립트로 사용할 모듈
    "outDir": "dist", //자바스크립트 파일이 생성될 경로
    "strict": true, //엄격한 타입 검사
    "moduleDetection": "force", //파일을 무엇으로 감지할것인가 (개별 파일) : export{} 키워드가 자동으로 추가됨
  },
  "ts-node": {
    "esm": true //ts-node 명령어를 ESModule에서 인식하게함, package.json에 "type" : "module" 설정 필요
  },
  "include": ["src"] //src 경로 아래 있는 모든 ts 파일을 컴파일
}